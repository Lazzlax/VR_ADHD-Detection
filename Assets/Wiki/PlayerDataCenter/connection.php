<?php
	$server = "localhost";		// Define the which server.
	$username = "root";			// Define the username.
	$password = "";				// Define the password.
	$db = "datacenter";			// Define the Database.
	
	$connect = new mysqli($server, $username, $password, $db); // Create connection into phpmyadmin.
?>